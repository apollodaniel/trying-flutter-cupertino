name: aprendendo_cupertino
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  mobx: ^2.2.3
  flutter_mobx: ^2.2.0+1
  email_validator: ^2.1.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  mobx_codegen: ^2.4.0
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
